#!/usr/bin/python3


def multiple_returns(sentence):
    """returns a tuple with the length of a string and its first character"""
    size = len(sentence)
    first_char = sentence[0] if len != 0 else "None"

    return (size, first_char)
